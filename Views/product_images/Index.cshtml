@model IEnumerable<EcommerceSite.Models.product_images>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.smallSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mediumSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.largeSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product1.label)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.smallSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mediumSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.largeSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.product1.label)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
